<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base -->
    <style name="Theme.IngeWeek" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Colores primarios -->
        <item name="colorPrimary">@color/md_theme_primary</item>
        <item name="colorPrimaryVariant">@color/md_theme_primaryContainer</item>
        <item name="colorOnPrimary">@color/md_theme_onPrimary</item>

        <!-- Colores secundarios -->
        <item name="colorSecondary">@color/md_theme_secondary</item>
        <item name="colorOnSecondary">@color/md_theme_onSecondary</item>

        <!-- Fondo -->
        <item name="android:windowBackground">@color/md_theme_background</item>
        <item name="android:colorBackground">@color/md_theme_background</item>

        <!-- Superficie -->
        <item name="colorSurface">@color/md_theme_surface</item>
        <item name="colorOnSurface">@color/md_theme_onSurface</item>

        <!-- Otros -->
        <item name="android:statusBarColor">@color/md_theme_primaryContainer</item>
        <item name="android:navigationBarColor">@color/md_theme_primaryContainer</item>

        <item name="android:fontFamily">sans-serif</item>
    </style>
    <style name="Theme.IngeWeek.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@drawable/uns</item>
        <item name="android:windowFullscreen">true</item>
    </style>

</resources>
